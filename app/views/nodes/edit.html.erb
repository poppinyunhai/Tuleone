<div class="row">
  <div class="span3 offset7">
    <%= form_for @node, :url => { :controller => 'nodes', :action => 'update', :id => @node } do |f| %>
    <%= f.label :name, t("index.name") %>
    <%= f.text_field :name %>
    <%= f.submit, t("index.update") %>
    <% end %>
  </div>
</div>